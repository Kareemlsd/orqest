# Baseline Python packages pre-installed in the per-session sandbox.
# The agent can pip-install more during a run; this set keeps Phase 4's
# benchmark scenario usable without network access for common scientific deps.

# Data + analysis
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4

# Plots + reports
matplotlib>=3.8
pillow>=10.3
weasyprint>=62.0     # Markdown → PDF in Phase 4
markdown>=3.6

# Web
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
readability-lxml>=0.8

# Shell WS server
websockets>=12.0
ptyprocess>=0.7

# Browser automation — Playwright Python binding. Chromium binary is
# already shipped by the base image under /ms-playwright.
playwright>=1.47,<1.48
